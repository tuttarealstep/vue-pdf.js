{
  "name": "@tuttarealstep/vue-pdf.js-monorepo",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --cwd ./packages/vue-pdfjs-playground dev",
    "build": "bun run --cwd ./packages/vue build",
    "test": "bun run --cwd ./packages/vue test",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "@tuttarealstep/vue-pdf.js": "file:./packages/vue"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "vitepress": "^1.5.0"
  },
  "peerDependencies": {
    "typescript": "^5.6.3"
  }
}